# NPM Configuration for optimal builds
# Reduces npm package verbosity
loglevel=warn

# Ensures proper peer dependency resolution
legacy-peer-deps=false

# Verify SSL certificates for security
strict-ssl=true
